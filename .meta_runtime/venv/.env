# NotebookLM Home — redirect all cookies/profiles into the workspace for portability.
# Path is relative to the workspace root. Tools resolve it through .meta_runtime/auth/.
NOTEBOOKLM_HOME=.meta_runtime/auth/notebooklm
