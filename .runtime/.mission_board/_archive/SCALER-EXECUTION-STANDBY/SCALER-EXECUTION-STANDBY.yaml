# 🎯 GOAL: SCALER-EXECUTION-STANDBY

metadata:
  name: SCALER-EXECUTION-STANDBY
  session: SES-EXECUTION-SCALER
  description: "Persistent standby goal for Scaler execution. Active when no specific operation is running. Awaits new discoveries (EXTERNAL) or internal gap audits (INTERNAL) to begin a new operation cycle."
  status: active 🟢
  priority: normal
  type: persistent ♾️

execution:
  plan:
    tasks:
      - Monitor EXTERNAL/discoveries/ for new unrouted items.
      - Monitor CONTROLER.yaml communication block for user-directed tasks.
      - On new input → follow Scaler-Workflows.md 5-phase approach.
      - On PLANNING mode approval received → proceed with queued integration.
    checkpoints:
      - Runbook read: Scaler-Architecture.md, Scaler-Workflows.md, Scaler-Operational-Rules.md, Scaler-Gateway.md [REQUIRED BEFORE EACH CYCLE]
      - Ledger consulted: EXTERNAL-LEDGER.yaml or INTERNAL-LEDGER.yaml [REQUIRED BEFORE EACH CYCLE]
      - Gateway card created before any integration [REQUIRED PER ACTION]
      - Double-entry sync after each action [REQUIRED PER ACTION]

  state:
    tracking: "Standby. Scaler architecture finalized 2026-05-12. Awaiting next task."
    artifacts: []
