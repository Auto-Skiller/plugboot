# Agentic OS v7 — backend recipe (not binaries).
# The venv rebuilds locally on first boot; only this recipe is committed.
starlette>=0.37
uvicorn>=0.29
ruamel.yaml>=0.18
jinja2>=3.1
python-multipart>=0.0.9
watchfiles>=0.21
