####################################################
# os-missions.yaml — missions for the _os entity
# Follows .infra/schemas/missions-schema.yaml + missions-templates.yaml
#
# Mission kinds: standard | research | evolution
# Evolution missions carry a `readiness` gate (Decisions #7): the agent must read
# the mission params + the evolution os_prompt, then flip the flags true before
# the daemon will let the evolution run advance.
####################################################

freshness:
  sync_status: fresh
  sync_count: 0
  last_synced: null
  last_edited: null

metrics:
  total: 0
  actives: 0

standard:
  "...":

research:
  "...":

evolution:
  FAST:
    "...":
  DEEP:
    "...":
  RESEARCH:
    "...":
  INBOX:
    "...":

archived:
  completed:
    "...":
  cancelled:
    "...":
