# 📋 ARCHITECTURE Discovery Sub-Ledger
# Tracks all discoveries (D, SD, SSD) originating from EXTERNAL/discoveries/architecture/.
# PREVENTION: Consult this ledger before processing any architecture/ item (anti-duplication law).
# PREVENTION: Any item routed out of .inbox/ must be logged here BEFORE being processed.

metadata:
  type: architecture
  description: "Granular tracking of all discoveries found in the architecture/ folder, including staged items routed from .inbox/."
  schema_version: "2.0"
  last_updated: ''

state:
  tracked_discoveries: []
  # Schema for each entry in tracked_discoveries:
  # - discovery_id: string        # e.g., DISC-ARCH-ROUTING-DESIGN
  #   source_path: string
  #   discovery_level: string     # D | SD | SSD | [deeper if applicable]
  #   parent_discovery_id: string # '' if D
  #   routed_from_random: bool    # true if routed from .inbox/
  #   discovery_type: string      # architecture (all items here are pre-typed)
  #   primary_aspect: string
  #   aspects:
  #     - string
  #   output_level: string        # architecture
  #   proposal_id: string
  #   card_path: string
  #   integration_type: string
  #   pending_proposal_ref: string
  #   user_decision: string
  #   action_gate_at_creation: string
  #   integration_status: string
  #   integrated_at: string
  #   scaler_notes: string

  summary:
    total_discoveries: 0
    pending: 0
    integrated: 0
    rejected: 0
