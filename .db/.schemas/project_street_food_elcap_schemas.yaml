---
metadata:
  name: project_street_food_elcap_schemas
  type: schema
  version: '1.0'
  schema_version: '1.0'
  purpose:
    entry_type: static
    value: Schema for project_street_food_elcap ledgers (business plan tracking)

project_street_food_elcap_ledger_schema: |
  metadata:
    name: string
    class: project_street_food_elcap
    type: ledger
    version: string
    schema_version: string
    freshness:
      status: active | syncing | error
      sync_count: integer
      last_synced_by: string
      last_synced: timestamp
  inputs:
    - board_goals_from: string
    - user_review_queue: string
  outputs:
    - completion_status_to: string
    - progress_to: string
  role: string
  audience: string
  priority: operational
  credentials:
    description: string
    when_to_use: string
    contains: [string]
  summary:
    total_files: integer
    completed_phases: integer
    tracked_items: [string]
  history: [string]
