---
name: "compare-options"
description: "Measure, compare, and optimize choices to ensure best path is taken."
triggers: ["benchmark", "compare", "measure", "optimize", "best option", "which is better"]
version: "1.0"
inputs: ["options", "criteria"]
outputs: ["comparison matrix", "optimal choice"]
cataloger_lock: false
---

# Compare Options

This skill allows the agent to measure, compare, and optimize choices to ensure the best path is taken.

## Steps

1. Identify the options to compare.
2. Define the criteria for evaluation.
3. Measure each option against the criteria.
4. Output the optimal choice based on the assessment.
