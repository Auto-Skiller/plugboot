# Generated by: EXTERNAL-SCALER-PIPELINE-10-ROUNDS (Phase 1 Classification)
# Source: pipeline_scaler/_SCALER-EXTERNAL_SOURCES/.scaler_mixed_inbox/browserbase skills/
# Created at: 2026-06-19T19:26:00+01:00

rejection:
  source_name: browserbase
  source_path: pipeline_scaler/_SCALER-EXTERNAL_SOURCES/.scaler_mixed_inbox/browserbase skills/
  destination_path: pipeline_scaler/_SCALER-EXTERNAL_SOURCES/.scaler_USER-SPACE/.complex_inboxes/browserbase/
  triggered_by: P-LAW-022
  trigger_signals:
    - signal: count
      detail: >
        11 sub-groups (autobrowse, browser, browser-trace, browserbase-cli, company-research,
        cookie-sync, event-prospecting, fetch, functions, search, ui-test) all belonging to the
        'browserbase' ecosystem — exceeds the 5-item threshold (complex_inbox_item_count_threshold: 5).
    - signal: structural_complexity
      detail: >
        The cluster has internal structure (sub-folders with SKILL.md-style contract files)
        bound to a single named ecosystem 'Browserbase'.
    - signal: cross_reference_coherence
      detail: >
        All sub-groups share Browserbase as their operational infrastructure reference,
        forming a coherent plugin-pack ecosystem bundle.
  item_count: 11
  agent_reasoning: >
    The 'browserbase skills' cluster is a complete ecosystem bundle of Browserbase-platform
    automation tools (browser automation, company research, cookie sync, UI testing, event
    prospecting, etc.). Classifying these per-item into OS pillars would lose the source
    coherence. Human triage is required to extract individual concerns (e.g., the browser
    automation concepts that could enrich our operational_toolboxes). Items moved whole to
    .complex_inboxes/browserbase/ pending user decision on how to extract utilities.
  rejected_at: '2026-06-19T19:26:00+01:00'
  ledger_marker: .db/pipeline_scaler.ledgers/.scaler_mixed_inbox.yaml
  next_steps: >
    User can extract individual items from .complex_inboxes/browserbase/ and re-drop
    them into .scaler_mixed_inbox/ or the appropriate typed _inbox/ folders for normal
    Scaler processing.
