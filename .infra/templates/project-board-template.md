# `<project_name>` - Project Board

> Entry point for `<project_name>`. Human-facing identity + notes. Live values live in `<project_name>-runtime.yaml`; work lives in `<project_name>-missions.yaml`.

## Project Identity
- Name: `<project_name>`
- Type: <!-- business | channel | codebase | legal-docs | content-op | ... -->
- Description: <!-- goals, scope, audience -->
- Current Phase: <!-- Discovery | Growth | Maintenance | ... -->

## Pillars (dynamic - mirror in runtime)
<!-- the focus areas this project cares about -->

## Data
The `<project_name>-data/` folder holds anything this project needs (codebase, business DB, content accounts, documents). Described by `<project_name>-data.yaml`.

<!-- On first creation, the agent asks the user for details, then tailors these sections to the project type. -->
